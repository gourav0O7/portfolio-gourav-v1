name = "gs-nda-gate"
main = "worker.js"
compatibility_date = "2026-07-01"

# Routes intentionally EMPTY — the NDA case studies are back to their
# original client-side experience (case-gate.js: scramble + decode-on-
# passphrase), so nothing is gated at the edge any more. The Worker code
# is kept here in case server-side gating is ever wanted again; re-add the
# route patterns and `wrangler deploy` to re-enable.
routes = []
