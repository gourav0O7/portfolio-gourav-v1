{
  "name": "portfolio-gourav-v1",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site. Sources live in the repo root and are served as-is by the default deploy; `npm run build` emits a minified copy into dist/ for the optional GitHub Actions deploy.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.31.0"
  }
}
